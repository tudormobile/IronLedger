<UserControl x:Class="AssetViewer.Views.AssetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AssetViewer.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Resources>
            <DataTemplate x:Key="componentData">
                <local:ComponentDataView DataContext="{Binding}"/>
            </DataTemplate>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Name:"/>
        <TextBox Grid.Row="1" Text="{Binding System.DisplayName}" />
        <GroupBox Grid.Row="2" Header="Description">
            <TextBox Text="{Binding System.Description}" TextWrapping="Wrap" Height="40"/>
        </GroupBox>
        <local:AssetIdView Grid.Row="3" DataContext="{Binding AssetId}"/>

        <TabControl Grid.Row="5">
            <TabItem Header="System">
                <ItemsControl ItemsSource="{Binding System.Data.Properties}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <local:ComponentPropertyView />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </TabItem>
            <TabItem Header="Processors">
                <ScrollViewer Grid.Row="5" CanContentScroll="False" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Processors}" ItemTemplate="{StaticResource componentData}"/>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="Memory">
                <ScrollViewer Grid.Row="5" CanContentScroll="False" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Memory}" ItemTemplate="{StaticResource componentData}"/>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="Disks">
                <ScrollViewer Grid.Row="5" CanContentScroll="False" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Disks}" ItemTemplate="{StaticResource componentData}"/>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
