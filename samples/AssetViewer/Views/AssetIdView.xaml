<UserControl x:Class="AssetViewer.Views.AssetIdView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AssetViewer.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid RowDefinitions="Auto,Auto,Auto">
        <!-- Top Row, System Metadata -->
        <local:AssetMetadataView DataContext="{Binding SystemMetadata}" />
        <!-- Middle Row, Baseboard/BIOS Metadata -->
        <WrapPanel Grid.Row="1">
            <GroupBox Header="Motherboard" Margin="4,4,4,0" Padding="2,0">
                <local:AssetMetadataView DataContext="{Binding BaseBoardMetadata}" />
            </GroupBox>
            <GroupBox Header="Bios" Margin="4,4,4,0" Padding="2,0">
                <local:AssetMetadataView DataContext="{Binding BiosMetadata}" />
            </GroupBox>
        </WrapPanel>
        <!-- Bottom Row, ID string -->
        <TextBlock Grid.Row="2" Text="{Binding Id}" Margin="6,0,0,0" Foreground="#80808080" d:Text="68e129f573a1758c8ec462e75d0234a30c46c9f8a99359d4c75cfb2a7f0ce4f7"/>
    </Grid>
</UserControl>
